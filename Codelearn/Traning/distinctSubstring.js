function distinctSubstring(s){
    let map = new Set();
    return map.size;
    for(let i = 1; i <= s.length; i++)
    {
        for(let j = 0; j <= s.length - i; j++)
    }
        {
        }
}
            map.add(s.substring(j, j+i));
 